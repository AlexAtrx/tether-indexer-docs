interface:
  display_name: "Address PR Comments"
  short_description: "Local-only PR review comments"
  default_prompt: "Use $address-pr-comments to review a PR locally and return only exact bad findings with paste-ready comments."

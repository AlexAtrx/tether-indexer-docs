# Missing context

- [ ] **Environments / systems:** Sentry org "Rumble", project `rumble-wallet-backend`, environment `production`. — **Need from Alex:** confirmation that this is the right project/env to be watching (vs. e.g. a `rumble-app-node` / `rumble-ork-wrk` / per-worker project), and access (or a viewer link) to the Sentry org so a future session can verify the empty state and the last event timestamp directly. **Source:** description (both screenshots).
- [ ] **External tickets / deploys:** Around 2026-05-01 something changed that stopped events from flowing. — **Need from Alex:** any deploy/PR/release that landed on or around May 1st in `rumble-wallet-backend` (or the host that runs it). A `git log --since=2026-04-25 --until=2026-05-05` window on the relevant repo would narrow it down. **Source:** description ("last task was May 1st").
- [ ] **Logs:** Status of the process(es) that own Sentry init on rumble-dev / prod. — **Need from Alex:** confirmation we can poke `rumble-dev` (or whichever environment the Sentry "production" env maps to) via the existing `access-dev-server` skill, plus pointer to where production prod logs live if different. **Source:** inferred from "Sentry not receiving" — need to see whether the process is up at all.
- [ ] **Sentry config:** DSN, `Sentry.init()` call, `tracesSampleRate`, inbound filters, release/environment tagging. — **Need from Alex:** which repo / file owns the Sentry init for `rumble-wallet-backend` (likely under `tetherto/rumble-app-node` or `tetherto/rumble-server`); confirm whether the empty-state's "inbound data filters" hint has been checked in the Sentry UI. **Source:** Sentry empty-state hint in screenshot 1.
- [ ] **Last surviving error context:** `Error — while open a file for lock: store/3002/db/LOCK: Perm…` from `RUMBLE-WALLET-BACKEND-2D`, frame `Object.onopen(r…`. — **Need from Alex:** the full Sentry issue page (stack trace, breadcrumbs, release, server name) — it may be the moment the process died and never restarted under a Sentry-instrumented entrypoint. **Source:** screenshot 2.
- [ ] **Project tag drift:** Screenshot 1 says project `rumble-wallet-backend`, screenshot 2's issue carries tag `RUMBLE-WALLET-BACKEND-2D`. — **Need from Alex:** confirmation whether `-2D` is a project rename / a different project entirely / a Sentry "short id" suffix. If a new project was created, events may now be landing there instead and the dashboard is just pointed at the old one. **Source:** screenshot 2.

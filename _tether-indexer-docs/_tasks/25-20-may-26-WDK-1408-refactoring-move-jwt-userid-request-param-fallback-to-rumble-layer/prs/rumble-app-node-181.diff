diff --git a/workers/http.node.wrk.js b/workers/http.node.wrk.js
index ae6b268..4d65889 100644
--- a/workers/http.node.wrk.js
+++ b/workers/http.node.wrk.js
@@ -7,7 +7,8 @@ const { ssoHandler, channelOwnershipHandler, docsBasicAuthHandler } = require('.
 const DocsGuard = require('./lib/middlewares/docs.guard')
 const { createRumbleResponseValidator } = require('./lib/middlewares/response.validator')
 const libServer = require('./lib/server')
-const { DefaultGuard, JwtGuard } = require('@tetherto/wdk-app-node/workers/lib/middlewares/auth')
+const { DefaultGuard } = require('@tetherto/wdk-app-node/workers/lib/middlewares/auth')
+const RumbleJwtGuard = require('./lib/middlewares/rumble.jwt.guard')
 const swagger = require('@fastify/swagger')
 const swaggerUi = require('@fastify/swagger-ui')
 const { testModeHandler } = require('@tetherto/wdk-app-node/workers/lib/middlewares')
@@ -56,7 +57,7 @@ class WrkServerHttp extends WdkServerHttpBase {
 
   async configureAuth () {
     middlewares.configureAuth('default', new DefaultGuard())
-    middlewares.configureAuth('secret', new JwtGuard(this.conf.jwtSecret))
+    middlewares.configureAuth('secret', new RumbleJwtGuard(this.conf.jwtSecret))
     middlewares.configureAuth('docs', new DocsGuard())
 
     // Register auth handlers
diff --git a/workers/lib/middlewares/rumble.jwt.guard.js b/workers/lib/middlewares/rumble.jwt.guard.js
new file mode 100644
index 0000000..eeb465c
--- /dev/null
+++ b/workers/lib/middlewares/rumble.jwt.guard.js
@@ -0,0 +1,11 @@
+'use strict'
+
+const { JwtGuard } = require('@tetherto/wdk-app-node/workers/lib/middlewares/auth')
+
+class RumbleJwtGuard extends JwtGuard {
+  _parsePayload (payload, req) {
+    return { id: payload.userId || req.params?.userId || req.query?.userId || req.body?.userId }
+  }
+}
+
+module.exports = RumbleJwtGuard

# Missing context

The ticket description points to two external sources that are not captured here.
Pull them before starting investigation.

- [ ] Slack thread: "Slack thread (full context, screenshots, log export): https://tether-to.slack.com/archives/C0A5DFYRNBB/p1779986078317469" — **Need from Alex:** export of the full thread, especially the screenshots and any log export Francesco attached there. The ticket description is a summary; the thread has the original timeline, who first noticed, and which boxes were restarted. **Source:** description.
- [ ] Logs (Grafana / Loki): "Grafana (pm2 errors, level=40): https://rwg.rmbl.ws/explore?..." — **Need from Alex:** the time-bounded log dump from that Grafana query (range `from=1779936172369` to `to=1779940187449`, roughly a ~67-minute window). The link is a Loki Explore URL — does it cover the full incident window, or only the start? Also: how often does `ERR_WALLET_TRANSFER_RPC_FAIL` fire in normal operation vs. during the incident — baseline vs. spike. **Source:** description.
- [ ] Environments / boxes: sample log shows `hostname=walletprd3` and shard worker `wrk-data-shard-proc-w-2-2-...` — **Need from Alex:** which prod boxes were affected (only `walletprd3`, or all three?), which shards (`-w-2-2` only, or others?), and the exact time window of the restart that mitigated. **Source:** description / log line.
- [ ] Incident timeline / decisions: "Status: currently mitigated via data shard restarts." — **Need from Alex:** when the first error fired, when restarts ran, whether the errors stopped immediately after restart, and whether any upstream cause (deploy, network event, peer dying) lined up with the start of the errors. **Source:** description.
- [ ] Related code / counterparty: error originates from `@hyperswarm/rpc/index.js:257` inside the shard worker calling a peer — **Need from Alex (or codebase check):** which service was the shard worker calling when the RPC closed? Most likely the chain indexer (`wdk-indexer-wrk-polygon`), but the log doesn't name the peer. Confirms whether the indexer was the one that disappeared. **Source:** stack trace in description.

# Missing context

The ticket body is a pasted Slack chat plus a log dump. The original Slack thread, the originating logs in Loki/Grafana, and the affected wallet/account context are all referenced but not linked.

- [ ] **Slack thread** — The description is a copy/paste of a chat between Andrei and Francesco (timestamps 7:31–8:39 PM on 2026-05-04). **Need from Alex:** link to the Slack permalink so we can see the full thread, the channel, and any later messages after Francesco said "I would need to check myself or with the team". **Source:** description.
- [ ] **Source of the error logs** — The error string `ERR_GET_TX_FROM_CHAIN_FAILED` / `[HRPC_ERR]=No such mempool or blockchain transaction. Use gettransaction for wallet transactions.` is shown but no link to Grafana / Loki / Cloud Logging. **Need from Alex:** the dashboard or query that produced this so we can see how widespread the error is, which wallet(s) / worker(s) are emitting it, and whether more than one tx hash is affected. **Source:** Andrei's 7:31 PM message.
- [ ] **Pasted log block is unrelated** — The "LOGS" section at the bottom of the description is a generic worker heartbeat dump (`finished processing N new messages`) from `tether-wallet-stg-0` at 2026-05-04 20:34:* — it does NOT contain the `ERR_GET_TX_FROM_CHAIN_FAILED` line. **Need from Alex:** the actual error log lines (with full context: wallet id, account id, retry count, the worker name) for the failing tx `86e0c91e…109e4a`. **Source:** description "LOGS" section.
- [ ] **Affected chain** — Hash `86e0c91ed20fccebf415f1fd201ba066549094fde793235818cc7cc335109e4a` is being looked up on `mempool.space` (Bitcoin mainnet), but the staging hostname in the log dump is `tether-wallet-stg-0` and we don't yet know if this tx is BTC mainnet, testnet, or another chain that uses the same `gettransaction` RPC verb. **Need from Alex:** confirmation that this is BTC mainnet (or which network) and which wallet/account holds the pending tx. **Source:** Andrei's 8:35 PM and 8:36 PM messages.
- [ ] **Wallet / account / database** — To craft a "delete one pending transaction" command, we need to know where the pending state lives: which HyperDB on which app-node / shard, the wallet id, the account id, and the schema for pending-tx records in Rumble. **Need from Alex:** which repo + DB owns pending-tx state for BTC in Rumble (likely `rumble-wallet` / `wdk-rumble-server` / btc-indexer), and which env/box Andrei should run the command from. **Source:** Francesco's 8:37 PM ask + the description's "command for Andrei to execute in production".
- [ ] **Scope of the fix request** — Francesco wrote: "we need to delete it and also have a fix to stop checking for it after some number of retries / time". This is *both* an ops command and a code change. **Need from Alex:** confirm whether RW-1699 covers only the ops command, or also the retry-cap fix (and if so, which repo's tx-watcher / pending-tx loop owns the retry policy). **Source:** Francesco's 8:37 PM message.
- [ ] **At-mention story payload** — The stories endpoint returned an `at_mention` system story (`gid 1214518658112761`) at 2026-05-04T18:42:08Z with no `text` / `html_text`. Probably the `@Alex Atrash` mention in the description, but worth confirming there isn't a hidden inline comment. **Need from Alex:** none, low priority — flag only if the ticket later seems to reference a comment we don't have. **Source:** stories.json.

External links to review:
- https://mempool.space/tx/86e0c91ed20fccebf415f1fd201ba066549094fde793235818cc7cc335109e4a — public mempool lookup; expected to return "tx not found" per the description.

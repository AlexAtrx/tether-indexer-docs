# Finalizing Transaction History API Contract

## Action Items

- Send final transaction‑history API contract JSON to the Slack channel today.
- After checking with Francisco and Vegan, provide a concrete deadline (by end‑of‑day) for fee data to appear in the API.
- Share the fee‑implementation timeline with the team within the next 2 hours.
- Deploy the new transaction‑history endpoint to the dev environment for internal testing, then plan staging rollout.
- Review and merge the pending 14 PRs (indexer, fee logic, transaction‑history changes).
- Record and post a short video showing the second and third rant scenarios working on v1.

## Transaction History API Contract

- Contract mirrors the previously agreed version, with two extra keys (fee‑related).
- Same structure across all networks; amount and totalAmount currently equal because fee is null.
- Request to return all fields (including fee) with zero values in the first round for implementation ease.
- Alex will share the JSON contract on Slack; team expects it ASAP.

## Fee Field Implementation

- Current backend returns amount = totalAmount; fee field is hard‑coded null.
- Plan: once indexer fetches real fee data, populate fee and adjust totalAmount (e.g., amount 10, fee 0.5, totalAmount 10.5).
- Need a timeline: target is this week, but exact date pending team confirmation.
- Fee data will be added to the same transaction object, not as a separate transaction.

## Deployment & Testing Strategy

- Deploy new transaction‑history endpoints first to dev so mobile can start testing.
- After internal QA, promote to staging; production rollout later.
- Only Alex has deployment keys now; will hand off next week.
- Suggest using Bruno or similar tool to hit the new endpoints for quick sanity checks.

## PR Review & Team Capacity

- 14 open PRs covering indexer changes, fee logic, and transaction‑history revamp.
- Team bandwidth limited (only Alex and Usman actively coding).
- Prioritize transaction‑history PRs; other work (e.g., Spark) can be deferred.

## Force Update (Expo vs Firebase)

- Goal: remote‑config‑driven forced update prompting users to install the new app version.
- Expo OTA updates can handle JavaScript changes; Firebase remote config already exists.
- Decision needed: reuse Firebase or implement via Expo’s expo-updates library.
- Estimated effort: a few hours for Expo implementation, plus access to EAS/Express.

## QR Code & Tip‑Jar UX

- Current QR handling defaults to the network with highest balance, preventing token change.
- Desired UX: after scanning, show tip‑jar name, logo, and allow user to select token/network.
- Two QR formats: plain address (app) vs. Rumble‑encoded link (web). Need unified handling or clear distinction.
- Backend may need to expose token‑address mapping per channel to enable token switching.

## Rant Scenario Implementation

- Only the first rant scenario works in v1; second and third are missing.
- Alex to verify feasibility on v1, record a demo video, and share with the team.
- Potential backend changes required; earlier investigation showed backend emits rant events.

## Cash‑Out Testing

- Patricia and Gohar to coordinate a joint testing session for cash‑out flow.
- Need a test account (virtual card) to share with Moonpay for troubleshooting.
- Plan a call today with Gohar to run through cash‑out steps together.

## General Launch Prep

- Release targeted for next week; transaction‑history and fee fixes are highest priority blockers.
- All team members encouraged to flag bugs in the channel promptly.
- Keep communication tight on Slack; post updates on any blockers or resource needs.

# Feature Flag Strategy for Spark & Plasma

## Action Items

- **Alex**: Add backend support for feature‑flag toggles (expose flag state via /config endpoint, cache in Redis).
- **Alex**: Run the swap‑flow test on staging after this call, capture video of the transaction list, and share it with the team.
- **Alex**: Execute the address‑query for migration (run Vegan’s query, get count of affected wallets, report back).
- **Alex**: Update Sentry configuration so errors are reported only in production builds (disable in dev/staging).
- **Alex**: Verify that the new transaction‑history API is merged and deployed to staging; if not, follow up with the PR owner.

## Feature‑Flag Strategy

- Centralize flag control via a remote‑config service (Firebase Remote Config or lightweight /config backend endpoint).
- Add separate flags for Spark and Plasma on each flow: tipping, send, receive, etc.
- Use Expo OTA updates only for version gating; actual flag values come from the remote config.
- Ensure the mobile app fetches flags at startup, caches locally, and hides UI elements when a flag is off.
- Coordinate with Andre (backend) to confirm flags won’t break Rumble’s front‑end.

## Swap Transaction Grouping

- Backend must aggregate raw transfer records (in, out, fee) into a single logical swap transaction.
- Grouping relies on matching transactionHash and parseTransferGroup() logic.
- Verify that the history endpoint returns one entry per swap, with underlyingTransfers\[\] populated.
- If multiple entries appear, inspect logs for grouping failures or token‑filter mismatches.

## Transaction‑History Endpoint Status

- v2 transaction‑history API is **not** yet in production or staging.
- Current observations come from the old flat‑transfer API, so grouping behavior may differ.
- Once the endpoint is live, re‑run swap‑flow tests to confirm correct aggregation.

## UI / UX Bugs Discussed

- Patricia’s dropdown menu scroll issue likely caused by bottom‑sheet swipe‑to‑close interaction.
- Send‑tip “Continue” button positioned above the keyboard; should be hidden below like in the Send screen.
- Onboarding notification auto‑navigates to Home, bypassing the “Continue/Skip” step – need to keep user on the same screen until they act.
- Spark address changes repeatedly on confirmation, causing rate‑limit errors; fix by stopping continuous generation requests.
- Numerical keyboard on iOS missing comma/period for locales – consider localization rather than removal.

## Sentry & Logging

- Restrict Sentry error reporting to production builds only; disable in dev and staging environments.
- Add masking for sensitive data (e.g., seed phrases) in logs, similar to existing v1 masking logic.
- Review all log statements for potential leakage of private information; raise any concerns in the chat.

## Regression & Release Testing

- Gohar to run a full regression on the current Android release (≈3 hrs).
- Skip Bitcoin‑related flows in regression until the new Bitcoin node is available.
- Verify that Spark and Plasma flags behave correctly in both web and mobile after the release.
- Ensure all critical bugs (dropdown, continue button, notification flow) are fixed before Monday code freeze.

## Bitcoin Server & Rate‑Limit Concerns

- Current Bitcoin client is rate‑limited; plan to provision a dedicated Bitcoin node for reliable indexing.
- Spark and Bitcoin clients both suffer from “no‑internet” crashes; prioritize fixing Spark address‑spamming bug first.

## Deployment & Release Timeline

- Spark transaction‑history code (by Vegan) pending PR, staging deployment imminent.
- Luganodes working on their side; expected rollout today or tomorrow.
- Plasma bug fix slated for the same release; will be enabled on staging after deployment.
- Android latest release already pushed; QA to start testing immediately.

## Migration & Keychain

- Migration logic must preserve keychain data; ensure no user data is erased during migration.
- Run query to count wallets with funds at risk (≈ $5 k total) and share results.
- Test migration on both Android (Firebase distribution) and iOS (potential Firebase OTA) once pass‑key support is ready.

## Miscellaneous

- Confirm that the transaction‑detail screen does **not** depend on the new transaction‑history API.
- Add a quick “remove initiated transfer” notification PR (backend only) – already merged.
- Ensure Expo OTA updates do not require a forced app update for flag changes.
- Keep communication open with George, Francesco, Patricia, and Andre for any blockers.
